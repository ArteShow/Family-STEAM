services:
  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "${FORNTEND_PORT}:${NGINX_PORT}"
    networks:
      - disknet

volumes:
  postgres_data:
  family-steam:
    name: family-steam

networks:
  disknet:
    driver: bridge
